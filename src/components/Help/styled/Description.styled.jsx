import styled from "styled-components";

export const Title = styled.h2`
  font-size: 22px;
  color: ${({ theme }) => theme.colors.fontBase};
  text-align: center;
  max-width: 640px;
  font-weight: 400;
  margin-bottom: 66px;
`;
